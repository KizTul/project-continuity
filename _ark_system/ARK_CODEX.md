# КОДЕКС ARK v2.5 (ARK v3.1)



## ГЛАВА 1: НАЗНАЧЕНИЕ



Этот документ является "энциклопедией" наших практических правил, стандартов и конвенций. Он отвечает на вопрос "КАКОВЫ ПРАВИЛА ВЫПОЛНЕНИЯ?".



---



## ГЛАВА 2: ОСНОВНОЙ РАБОЧИЙ ЦИКЛ ARK v3.0



Это — стандартный, единственно верный процесс для взаимодействия в рамках системы ARK.



1.  **Начало Сессии:**

    *   **Действие:** Запустите `START_SESSION.bat`.

    *   **Результат:** Полный стратегический контекст копируется в буфер обмена.

    *   **Действие:** Инициализируйте ИИ-Исполнителя.



2.  **Внесение Изменений:**

    *   **Действие:** ИИ генерирует "Пакет Модификаций" в формате JSON.

    *   **Действие:** Напарник сохраняет пакет в `_ark_system/_staging/modification_package.json`.

    *   **Действие:** Напарник запускает `ARK_Apply_Changes.bat` для применения изменений.

    *   **Действие (Обратная связь):** Напарник передает ИИ "Транзакционную Квитанцию". **ИИ обязан немедленно обновить свою внутреннюю модель состояния на основе данных из квитанции (включая `new_checksum` для измененных файлов), независимо от того, была ли транзакция успешной или провальной. Это ключевой шаг для предотвращения рассинхронизации.**



3.  **Цикл Обучения ("Аудит"):**

    *   **Событие:** При возникновении "значимого события" инициируется работа с ИИ-Аудитором.

    *   **Процесс:** Описан в `OPERATING_PROCEDURES.md`, процедура `#PROC-EXP-6`.



4.  **Синхронизация:**

    *   **Действие:** Все изменения должны быть синхронизированы с GitHub с помощью `ARK_Sync_System.bat`.



---



## ГЛАВА 3: СТАНДАРТНАЯ СТРУКТУРА ПРОЕКТА



projects/

└── [ProjectName]/

    ├── _docs/      # Документация (GDD, Roadmap, Specs)

    ├── _src/       # Исходный код

    ├── _tools/     # Специфичные для проекта инструменты

    └── _references/ # Референсы, ассеты



---



## ГЛАВА 4: СТАНДАРТЫ УПРАВЛЕНИЯ ВЕРСИЯМИ (GIT)



### #GS-1: Принцип Управления Секретами

*   **Правило:** Все секретные данные (API-ключи, токены, пароли) **ЗАПРЕЩЕНО** хранить в системе контроля версий (Git). Файлы `.env` **обязаны** быть включены в корневой `.gitignore`.

*   **Практика для локальной разработки:** Использовать файлы `.env`.

*   **Практика для развертывания:** Использовать встроенные механизмы хостинга (Secrets).



### #GS-3: Стандарт Коммитов

*   **Правило:** Все коммиты должны следовать спецификации **Conventional Commits**.

*   **Формат:** `type(scope): description`

*   **META-коммиты:** Изменения в "Документах Ядра" и "Кодексах Опыта" фиксируются с типом `meta`. *Пример: `meta: усилен принцип #CP-1`.*



---



## ГЛАВА 5: СТАНДАРТЫ КАЧЕСТВА КОДА



### #KQ-1: Протокол Финальной Верификации ("Цербер")

| Уровень | Критерии | Действие при сбое |

| :--- | :--- | :--- |

| **1. Синтаксис** | Код не содержит синтаксических ошибок. | Немедленное исправление. |

| **2. Логика** | Код решает поставленную задачу в соответствии с Планом. | Анализ ошибки, возврат к Плану. |

| **3. Интеграция** | Код корректно взаимодействует с другими частями проекта. | Выдача с пометкой `⚠ Контекстная интеграция неполна`. |



### #KQ-2: Протокол Отказоустойчивости ("Атлант")

*   **Описание:** Код, работающий с внешними данными, должен включать обработку ошибок.

*   **Требования:**

    *   Проверки на `null` или `is_instance_valid()`.

    *   Обработка ошибок загрузки ресурсов (`try-except` или аналоги).

*   **Стандартная реакция при ошибке:** Логирование ошибки и возврат безопасного значения по умолчанию.



### #KQ-5: Статический Анализ Кода

*   **Правило:** Для каждого проекта должен быть настроен и использоваться соответствующий линтер (например, `pylint` для Python). Код с ошибками уровня `Error`, обнаруженными линтером, не может быть принят в `develop` ветку.



---



## ГЛАВА 6: СТАНДАРТЫ ИНТЕГРАЦИИ С ИНСТРУМЕНТАМИ



### #TI-3: Стандарт Интерактивных Инструментов

*   **Правило:** Все `.bat`-инструменты должны использовать `chcp 65001 > nul` и `pause`.



### #TI-4: Протокол Атомарного Обновления (Git Bash)

*   **СТАТУС: 🟥 УСТАРЕЛ И ЗАПРЕЩЕН К ИСПОЛЬЗОВАНИЮ.**

*   **Вердикт Аудита:** Данный протокол признан проваленным из-за фундаментальной хрупкости канала передачи команд.



### #TI-5: Протокол "Транзакционный Пакет Модификаций" v1.1 (✅ ДЕЙСТВУЮЩИЙ СТАНДАРТ)



#### 1. Формат "Пакета Модификаций" (JSON Schema v1.1)



Пакет должен быть **JSON-объектом**, содержащим ключи `"version"` и `"modifications"`.



**Спецификация Полей Операций:**

*   `action` (string, **обязательно**): Тип операции. (`UPPER_SNAKE_CASE`). Допустимые значения: `CREATE_DIRECTORY`, `DELETE_DIRECTORY`, `CREATE_FILE`, `MODIFY_FILE`, `DELETE_FILE`, `APPEND_TO_FILE`, `APPEND_TO_JSON_ARRAY`, `REPLACE_IN_FILE`.

*   `path` (string, **обязательно**): Относительный путь.

*   `expected_checksum_before` (string, опционально): Контрольная сумма файла **до** изменений. **Обязательно** для всех операций, изменяющих существующие файлы.

*   *Прочие поля (`content`, `payload`, `pattern`, `replacement`) используются по необходимости в зависимости от `action`.*



#### 2. Требования к Инструменту-Обработчику (`apply_modifications.py v3.2`)

Инструмент **обязан** реализовывать следующие принципы, подтвержденные аудитом:

1.  **Безопасность и Валидация:** Проверка путей, валидация схемы JSON, проверка версии пакета.

2.  **Проверка Целостности Состояния:** (`expected_checksum_before`).

3.  **Атомарность Записи:** Все операции изменения/создания файлов должны выполняться через временный файл и атомарную операцию `os.replace()` для исключения частичной записи.

4.  **Надежный Откат (Rollback):** При сбое любой операции в пакете, инструмент обязан **полностью** отменить все **уже выполненные** операции в рамках транзакции, возвращая файловую систему в исходное состояние.

5.  **Блокировка (Locking):** Использование lock-файла (`.apply.lock`) для предотвращения одновременного запуска.

6.  **Стандарт Верификации:** Критические изменения в инструменте должны верифицироваться с помощью "провокационного теста", который намеренно вызывает сбой для проверки корректности отката.

7.  **Автоматическое Управление Контрольными Суммами:**

    *   **Правило:** После **каждой успешной** операции `MODIFY_FILE` над `.md` файлом, который должен содержать тег контрольной суммы, инструмент **обязан автоматически пересчитать** SHA-256 чистого содержимого и **обновить или добавить** в конец файла корректный тег `<!-- [ARK_INTEGRITY_CHECKSUM::sha256:...] -->`.

    *   **Цель:** Снять с ИИ и Напарника ответственность за ручное управление контрольными суммами, гарантируя их постоянную актуальность.



### #TI-6: Стандарт Безопасного Создания и Редактирования Артефактов v1.0 (✅ ДЕЙСТВУЮЩИЙ СТАНДАРТ)



*   **Назначение:** Установить единый, отказоустойчивый стандарт для создания и редактирования ВСЕХ текстовых файлов в экосистеме ARK, чтобы гарантировать их техническую корректность и предсказуемость.

*   **Принцип:** Любой текстовый артефакт (код, документ, данные) должен создаваться и редактироваться с полным контролем над его техническими параметрами (кодировка, концы строк).



#### 1. Основные Положения

*   **Единственный Утвержденный Инструмент:** **Visual Studio Code (VS Code)**.

*   **Запрещенный Инструмент:** Стандартный "Блокнот" Windows (`notepad.exe`).

*   **Источник Правды:** Единственным источником правды о техническом состоянии файла является **строка состояния в правом нижнем углу окна VS Code**.



#### 2. Стандартные Параметры по Типам Файлов

| Расширение                | Кодировка | Концы строк (EOL) | Тип языка (в VS Code) |

| :------------------------ | :-------- | :---------------- | :-------------------- |

| `.md`, `.txt`, `.json`    | `UTF-8`   | `CRLF`            | *Не требуется*        |

| `.bat`                    | `UTF-8`   | `CRLF`            | `Batch`               |

| `.py`                     | `UTF-8`   | `LF`              | `Python`              |

| *Прочие текстовые файлы*    | `UTF-8`   | `CRLF`            | *Определяется по типу*|



#### 3. Шаблон Строгой Директивы для ИИ-Исполнителя

*   **Обязательство:** ИИ-Исполнитель ("Прометей") обязан предоставлять инструкции по созданию или редактированию файлов в строгом соответствии со следующим шаблоном, выводя его в виде единого, связного абзаца перед блоком с артефактом.

*   **Шаблон:** "Откройте Visual Studio Code и [создайте/откройте] файл **[ПУТЬ/ИМЯ_ФАЙЛА]**. Вставьте/измените содержимое, используя предоставленный ниже блок. Перед сохранением **обязательно** убедитесь, что в строке состояния (правый нижний угол) установлены параметры: кодировка **[КОДИРОВКА]** и концы строк **[ФОРМАТ EOL]**. После этого сохраните файл (`Ctrl+S`) и [СЛЕДУЮЩИЙ ШАГ]."



---



## ГЛАВА 7: CHANGELOG

*   **v2.4 (28.08.2025):**

    *   **META:** Документ восстановлен до полной версии. Интегрирован стандарт #TI-6 и восстановлены главы (4, 5) из архива.

*   **v2.3 (28.08.2025):**

    *   **META:** Добавлено требование (#TI-5, п.7) к инструменту по автоматическому управлению контрольными суммами для обеспечения целостности Ядра.

*   **v2.2 (26.08.2025):**

    *   **META:** Усилен протокол #TI-5, добавлены требования к инструменту на основе аудита отказоустойчивости. Обновлен основной рабочий цикл.

*   **v2.1 (26.08.2025):** Восстановлен полный текст документа.

*   **v2.0 (26.08.2025):** КРУПНЫЙ РЕФАКТОРИНГ.



---

## ГЛАВА 8: СТАНДАРТЫ ПРОЕКТИРОВАНИЯ ОТКАЗОУСТОЙЧИВЫХ ИНСТРУМЕНТОВ



### #TS-1: Назначение

Этот стандарт является **обязательным к исполнению** при проектировании или модификации любого инструмента (скрипта), который выполняет операции с файловой системой. Его цель — гарантировать транзакционность, целостность данных и предсказуемость поведения инструмента при сбоях.



### #TS-2: Ключевые Требования



Любой инструмент, подпадающий под действие данного стандарта, **обязан** реализовывать следующие четыре принципа:



**1. Атомарность Записи:**

*   **Правило:** Любая операция по изменению или созданию файла должна выполняться по схеме "запись во временный файл -> атомарная замена (`os.replace`)".

*   **Цель:** Полностью исключить риск повреждения данных ("частичная запись") в случае аварийного завершения работы скрипта. Файл на диске должен существовать либо в старом, либо в новом состоянии, без промежуточных.



**2. Предварительное Журналирование Отката (Write-Ahead Undo Log):**

*   **Правило:** Перед выполнением любой файловой операции, инструкция по ее полной отмене (например, "восстановить файл X из бэкапа Y") должна быть записана в лог транзакции.

*   **Цель:** Гарантировать 100% возможность отката к исходному состоянию, даже если сбой произойдет в самый критический момент выполнения операции.



**3. Механизм Блокировки (Locking):**

*   **Правило:** В начале своей работы инструмент должен создавать эксклюзивный lock-файл. Если такой файл уже существует, инструмент обязан немедленно завершить работу с ошибкой. Lock-файл должен гарантированно удаляться после завершения работы (как успешной, так и провальной).

*   **Цель:** Предотвратить состояние гонки и повреждение данных, которое может возникнуть при случайном одновременном запуске двух копий инструмента.



**4. Верификация через "Провокационный Тест":**

*   **Правило:** Финальным этапом сдачи любого нового или значительно измененного инструмента является успешное прохождение "Провокационного Теста". Тест должен намеренно симулировать сбой в середине многошаговой транзакции.

*   **Цель:** Доказать, что механизм отката (rollback) полностью функционален и способен вернуть систему в чистое, исходное состояние после сбоя.



---

## ГЛАВА 9: СТАНДАРТ БЕЗОПАСНОГО ОБНОВЛЕНИЯ КРИТИЧЕСКИХ ИНСТРУМЕНТОВ (НОВЫЙ)



### #TU-1: Назначение

Этот стандарт является **обязательным к исполнению** при необходимости обновить критически важные инструменты системы ARK (например, `apply_modifications.py`). Его цель — обеспечить максимальную безопасность, предсказуемость и минимизировать риски, связанные с модификацией "живых" компонентов системы.



### #TU-2: Ключевые Принципы



1.  **ЗАПРЕТ САМОМОДИФИКАЦИИ:** Инструменту **категорически запрещено** изменять собственный исходный код в процессе выполнения. Это нестабильная и непредсказуемая операция.

2.  **ПРИНЦИП "UPDATER VS TARGET":** Обновление всегда должно выполняться **отдельным, внешним процессом** (Updater), который воздействует на целевой инструмент (Target).



### #TU-3: Протокол Обновления



**Шаг 1: Подготовка**

*   **1.1.** Остановить все активные процессы, которые могут использовать обновляемый инструмент.

*   **1.2.** Создать ручную резервную копию обновляемого файла (например, `apply_modifications.py` -> `apply_modifications.py.bak`).



**Шаг 2: Предварительная Проверка (Pre-flight Check)**

*   **2.1.** **Проверка синтаксиса:** Обязательно проверить новый код на синтаксическую корректность с помощью встроенного компилятора Python. **Команда:** `python -m py_compile [путь_к_новому_файлу]`.

*   **2.2.** **Внешний Аудит:** Для сложных изменений — следовать протоколу `#PROC-EXP-7`.



**Шаг 3: Атомарная Замена**

*   **3.1.** Использовать ручную замену (копирование-вставка) или отдельный, простой скрипт-обновлятор для замены старого файла новым.



**Шаг 4: Финальная Верификация (Post-flight Check)**

*   **4.1.** Провести безопасный "Canary Test": выполнить простой, неразрушающий "Пакет Модификаций" (например, создание временного файла), чтобы доказать, что обновленный инструмент функционирует корректно.



**Шаг 5: Очистка**

*   **5.1.** После успешной верификации удалить ручную резервную копию (`.bak`).

## ГЛАВА 10: СТАНДАРТЫ КОММУНИКАЦИИ
### **#CS-1: Стандарт Коммуникации и Форматирования Ответов v1.0**

*   **Цель:** Обеспечить предсказуемость, машиночитаемость и удобство использования ответов ИИ в среде Google AI Studio.
*   **Принцип:** Каждый тип информации должен быть представлен в своем, предназначенном для этого формате.

**1. Простой Текст (Объяснения, Планы, Вопросы):**
*   **Формат:** Обычный текст, без специального форматирования.
*   **Пример:**
    > Принято. План утвержден. Приступаю к генерации артефакта.

**2. Артефакты для Копирования (Команды, JSON, Код):**
*   **Формат:** **"Code block"**. Это блок с темным фоном и кнопкой "Copy code" в правом верхнем углу. Артефакт должен быть "стерильным" — без сопроводительного текста внутри блока.
*   **Пример (Команда):**
    ```
    python _ark_system/_tools/get_checksum.py _ark_system/some_file.md
    ```
*   **Пример (JSON):**
    ```json
    {
      "version": "1.1",
      "modifications": [
        {
          "action": "CREATE_FILE",
          "path": "path/to/file.txt",
          "content": "Hello, World!"
        }
      ]
    }
    ```

**3. Смешанный Контент (Документация Markdown):**
*   **Формат:** Для передачи `.md` файлов, которые содержат и текст, и вложенные блоки кода, используется один общий **"Code block"** с указанием типа `markdown`.
*   **Пример:**
    ```markdown
    # Заголовок Документа
    
    Это параграф обычного текста.
    
    ```python
    # А это вложенный блок кода
    print("Hello, World!")
    ```
    ```

**Критическая Директива:** Смешивание простого текста и "Code block" в одном ответе разрешено, но сам "Code block" должен оставаться "стерильным".

*   **❌ НЕПРАВИЛЬНО:**
    ```
    Вот ваша команда:
    python --version
    ```
*   **✅ ПРАВИЛЬНО:**
    Вот ваша команда:
    ```
    python --version
    ```

<!-- [ARK_INTEGRITY_CHECKSUM::sha256:d8a577c449339e8c3b772c9e7826279930f6a2731de86f505342a08f52077e23] -->